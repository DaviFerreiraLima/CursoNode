const moduloSoma = require('../soma')
const minimist = require ('./minimist')


//modulo externo
//const args = minimist(process.argv.slice(2))

//modulo interno
const soma = moduloSoma.soma 

const args= minimist(process.argv.slice(2))

const a = parseInt(args['a'])

const b = parseInt(args['b'])

soma(a,b)

